import MyProfileClient from "@/components/MyProfileClient";

export const metadata = {
  title: "My Profile — TileVista",
};

export default function MyProfilePage() {
  return <MyProfileClient />;
}
